// Controllers básicos para el backend
// Este archivo se puede expandir según necesidades

export class ProductController {
  // Métodos del controlador de productos
}

export class BrandController {
  // Métodos del controlador de marcas
}

export class CategoryController {
  // Métodos del controlador de categorías
}

export class DashboardController {
  // Métodos del controlador del dashboard
}
